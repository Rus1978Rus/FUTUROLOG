# IMPLEMENTATION ORDER

## Phase 0 — Verification

1. Получить исходные архивы от автора.
2. Проверить структуру кода.
3. Закрыть все `[REQUIRES_VERIFICATION]` в M1.1.
4. Создать ветку `feature/m1-domain-neutral`.

## Milestone M1 — Entropy-RG v3.0-domain-neutral

1. M1.1 — migration plan.
2. M1.2 — domain rename: `seller/listing/category → actor/event/topic`.
3. M1.3 — canonical scoring rename + `trust_adjustment`.
4. M1.4 — dual output + metadata + gamma 0.07.
5. M1.5 — final documentation.

## Milestone M3 — Objective Layer

1. M3.1 — `temporal_persistence`.
2. M3.2 — `observer_agreement`.
3. M3.3 — `source_redundancy`.
4. M3.4 — `noise_separation`.
5. M3.5 — `scale_stability`.
6. M3.6.1 — activation math.
7. M3.6.2 — operational readiness.

## Deferred milestones

- M2 — `flow_asymmetry`, split `local_action_risk` and `profile_deviation`.
- M4 — ENRA integration.
- M5 — calibration.
- M6 — hash-chain MVP.
- M7 — source adapters.
- M8 — dashboard.
- M9 — human review loop.
