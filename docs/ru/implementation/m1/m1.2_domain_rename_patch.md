# M1.2 Domain Rename Patch

# Доменное переименование Entropy-RG v2.2: seller/listing → actor/event

Статус: patch-документ для M1.2  
Проект: «Футуролог»  
Основание: `migration_plan_v3_0.md`  
Цель: убрать маркетплейсную привязку из кода и данных без изменения scoring-логики

Важно: этот patch-документ подготовлен по согласованному плану M1.1 и заданной таблице переименований. Фактический архив `entropy_rg_anomaly_detector_EN_v2_2_FINAL.zip` в текущем контексте не открыт, поэтому места, где требуется сверка с реальным кодом, помечены `[REQUIRES_VERIFICATION]`.

## Сводка

M1.2 выполняет только доменное переименование Entropy-RG: `seller/listing/category/region` и связанные feature-поля переводятся в `actor/event/topic/location`. Также переименовываются CSV-файлы `data/sellers.csv → data/actors.csv` и `data/listings.csv → data/events.csv`, добавляются новые API endpoints `GET /actor/{id}` и `GET /event/{id}`, а старые endpoints сохраняются как deprecated aliases до v3.2. Scoring keys, `DEFAULT_WEIGHTS`, формулы, пороги, confluence-логика, HMAC-логика, `SYSTEM_META`, `model_version`, `weights_version`, dual output и canonical scoring rename в M1.2 не изменяются. Ожидаемо затрагиваются файлы `app/scoring.py`, `app/confinement.py`, `app/sequential.py`, `app/large_deviation.py`, `app/trusted_baseline.py`, `app/security.py`, `app/explanations.py`, `app/calibration.py`, `app/api.py`, `app/main.py`, `app/dashboard.py`, `app/state_store.py`, CSV-файлы, тесты и документация. Точный список должен быть сверён после распаковки архива. [REQUIRES_VERIFICATION]

## Обнаруженные дополнительные имена

Архив с фактическим кодом не был открыт, поэтому автоматическое обнаружение дополнительных имён не выполнено. Дополнительных имён не обнаружено на основе предоставленной таблицы, но после распаковки архива разработчик обязан выполнить поиск:

```bash
grep -R "seller\|listing\|category\|region\|reviews_count\|rating\|account_age_days\|verified\|avg_price_deviation\|posting_frequency\|duplicate_image_ratio\|contact_reuse_score" -n app data tests README.md docs || true
```

Если будут найдены имена вне таблицы, добавить их сюда:

| Старое имя | Где встречается | Предлагаемое новое имя | Статус |
|---|---|---|---|
| `[REQUIRES_VERIFICATION]` | `[REQUIRES_VERIFICATION]` | `[OPEN_QUESTION]` | `[OPEN_QUESTION]` |

Примеры возможных дополнительных имён, которые нельзя переименовывать без ревью:

| Старое имя | Где может встречаться | Предлагаемое новое имя | Статус |
|---|---|---|---|
| `seller_name` | CSV/API/dashboard | `actor_name` | [OPEN_QUESTION] |
| `listing_title` | CSV/dashboard | `event_title` или `title` | [OPEN_QUESTION] |
| `listing_description` | CSV/dashboard | `event_description` или `description` | [OPEN_QUESTION] |
| `seller_profile` | docs/code | `actor_profile` | [OPEN_QUESTION] |
| `marketplace` | README/docs | `reference domain` | [OPEN_QUESTION] |

## Изменения по файлам

## app/scoring.py

Назначение изменения: доменные имена в function signatures, payload fields, feature extraction и return payload переводятся на `actor/event/topic/location`. Scoring keys не трогаются.

### Публичные функции

```python
# БЫЛО
def score_seller(seller: dict, listings: list[dict], baseline: dict | None = None) -> dict:
    seller_id = seller.get("seller_id")
    seller_listings = listings
    ...
    return {
        "seller_id": seller_id,
        "component_scores": component_scores,
        "final_score": final_score,
        "reasons": reasons,
    }


# СТАЛО
def score_actor(actor: dict, events: list[dict], baseline: dict | None = None) -> dict:
    actor_id = actor.get("actor_id")
    actor_events = events
    ...
    return {
        "actor_id": actor_id,
        "component_scores": component_scores,
        "final_score": final_score,
        "reasons": reasons,
    }


# DEPRECATED ALIAS — удалить в v3.2
score_seller = score_actor
```

```python
# БЫЛО
def score_listing(listing: dict, seller_context: dict, baseline: dict | None = None) -> dict:
    listing_id = listing.get("listing_id")
    seller_id = listing.get("seller_id")
    category_id = listing.get("category_id")
    region = listing.get("region")
    ...


# СТАЛО
def score_event(event: dict, actor_context: dict, baseline: dict | None = None) -> dict:
    event_id = event.get("event_id")
    actor_id = event.get("actor_id")
    topic_id = event.get("topic_id")
    location = event.get("location")
    ...


# DEPRECATED ALIAS — удалить в v3.2
score_listing = score_event
```

### Feature extraction

```python
# БЫЛО
account_age_risk = compute_account_age_risk(seller.get("account_age_days"))
rating_risk = compute_rating_risk(seller.get("rating"))
reviews_risk = compute_reviews_risk(seller.get("reviews_count"))
contact_reuse_score = seller.get("contact_reuse_score", 0.0)
posting_frequency = seller.get("posting_frequency", 0.0)


# СТАЛО
actor_age_risk = compute_actor_age_risk(actor.get("actor_age_days"))
reputation_risk = compute_reputation_risk(actor.get("reputation_score"))
endorsement_risk = compute_endorsement_risk(actor.get("endorsement_count"))
identity_reuse_score = actor.get("identity_reuse_score", 0.0)
emission_frequency = actor.get("emission_frequency", 0.0)
```

```python
# БЫЛО
avg_price_deviation = listing.get("avg_price_deviation", 0.0)
duplicate_text_ratio = listing.get("duplicate_text_ratio", 0.0)
duplicate_image_ratio = listing.get("duplicate_image_ratio", 0.0)


# СТАЛО
avg_value_deviation = event.get("avg_value_deviation", 0.0)
duplicate_text_ratio = event.get("duplicate_text_ratio", 0.0)
duplicate_media_ratio = event.get("duplicate_media_ratio", 0.0)
```

### Helper functions

```python
# БЫЛО
def compute_account_age_risk(account_age_days: float) -> float:
    ...


# СТАЛО
def compute_actor_age_risk(actor_age_days: float) -> float:
    ...


# DEPRECATED ALIAS — удалить в v3.2
compute_account_age_risk = compute_actor_age_risk
```

```python
# БЫЛО
def compute_rating_risk(rating: float) -> float:
    ...


# СТАЛО
def compute_reputation_risk(reputation_score: float) -> float:
    ...


# DEPRECATED ALIAS — удалить в v3.2
compute_rating_risk = compute_reputation_risk
```

```python
# БЫЛО
def compute_reviews_risk(reviews_count: int) -> float:
    ...


# СТАЛО
def compute_endorsement_risk(endorsement_count: int) -> float:
    ...


# DEPRECATED ALIAS — удалить в v3.2
compute_reviews_risk = compute_endorsement_risk
```

### Aggregation helpers

```python
# БЫЛО
def count_unique_regions(listings: list[dict]) -> int:
    return len({row.get("region") for row in listings if row.get("region")})


# СТАЛО
def count_unique_locations(events: list[dict]) -> int:
    return len({row.get("location") for row in events if row.get("location")})


# DEPRECATED ALIAS — удалить в v3.2
count_unique_regions = count_unique_locations
```

```python
# БЫЛО
def count_unique_categories(listings: list[dict]) -> int:
    return len({row.get("category") for row in listings if row.get("category")})


# СТАЛО
def count_unique_topics(events: list[dict]) -> int:
    return len({row.get("topic") for row in events if row.get("topic")})


# DEPRECATED ALIAS — удалить в v3.2
count_unique_categories = count_unique_topics
```

```python
# БЫЛО
def aggregate_duplicate_image_ratio(listings: list[dict]) -> float:
    ...


# СТАЛО
def aggregate_duplicate_media_ratio(events: list[dict]) -> float:
    ...


# DEPRECATED ALIAS — удалить в v3.2
aggregate_duplicate_image_ratio = aggregate_duplicate_media_ratio
```

### Не менять в M1.2

```python
# НЕ ТРОГАТЬ В M1.2
DEFAULT_WEIGHTS = {
    ...
}
```

```python
# НЕ ТРОГАТЬ В M1.2
component_scores = {
    "gibbs": ...,
    "residue": ...,
    "sequential": ...,
    "surprise": ...,
    "graph": ...,
    "trust": ...,
    "robustness": ...,
}
```

Формулы не изменены: да  
Ключи DEFAULT_WEIGHTS не изменены: да

## app/confinement.py

Назначение изменения: заменить domain terms `category/listing/seller/region` на `topic/event/actor/location`, переименовать coupling constant, оставить значения reference-domain profile без изменений.

### Coupling constant

```python
# БЫЛО
CATEGORY_COUPLING = {
    "electronics": 1.00,
    "phones": 1.10,
    "cars": 1.25,
    "real_estate": 1.35,
}


# СТАЛО
TOPIC_COUPLING = {
    # Reference domain examples, replace with your domain topics.
    "electronics": 1.00,
    "phones": 1.10,
    "cars": 1.25,
    "real_estate": 1.35,
}


# DEPRECATED ALIAS — удалить в v3.2
CATEGORY_COUPLING = TOPIC_COUPLING
```

### Expected decay

```python
# БЫЛО
def compute_expected_decay(scale_distance: float, category: str) -> float:
    coupling = CATEGORY_COUPLING.get(category, 1.0)
    ...


# СТАЛО
def compute_expected_decay(scale_distance: float, topic: str) -> float:
    coupling = TOPIC_COUPLING.get(topic, 1.0)
    ...
```

### Information residue check

```python
# БЫЛО
def check_information_residue(listings: list[dict], seller: dict, category: str) -> dict:
    observed = compute_observed_residue(listings, seller)
    expected = compute_expected_decay(scale_distance, category)
    return {
        "seller_id": seller.get("seller_id"),
        "category": category,
        "residue": residue,
    }


# СТАЛО
def check_information_residue(events: list[dict], actor: dict, topic: str) -> dict:
    observed = compute_observed_residue(events, actor)
    expected = compute_expected_decay(scale_distance, topic)
    return {
        "actor_id": actor.get("actor_id"),
        "topic": topic,
        "residue": residue,
    }
```

Примечание: `residue` как scoring key не переименовывается в M1.2.

### Deprecated alias for public function

```python
# БЫЛО
def compute_observed_residue(listings: list[dict], seller: dict) -> float:
    ...


# СТАЛО
def compute_observed_residue(events: list[dict], actor: dict) -> float:
    ...


# DEPRECATED ALIAS — удалить в v3.2
compute_seller_residue = compute_observed_residue  # [REQUIRES_VERIFICATION]
```

Если фактическое имя старой публичной функции отличается, alias должен сохранять именно старое имя. [REQUIRES_VERIFICATION]

Формулы не изменены: да  
Ключи DEFAULT_WEIGHTS не изменены: да

## app/sequential.py

Назначение изменения: заменить subject/event identifiers в sequence state без изменения sequential scoring logic.

### Sequence update

```python
# БЫЛО
def update_seller_sequence(
    seller_id: str,
    listing_id: str,
    score: float,
    timestamp: str,
    state_store,
) -> dict:
    previous_state = state_store.get_sequence_state(seller_id)
    ...


# СТАЛО
def update_actor_sequence(
    actor_id: str,
    event_id: str,
    score: float,
    timestamp: str,
    state_store,
) -> dict:
    previous_state = state_store.get_sequence_state(actor_id)
    ...


# DEPRECATED ALIAS — удалить в v3.2
update_seller_sequence = update_actor_sequence
```

### Sequence events

```python
# БЫЛО
new_state = {
    "seller_id": seller_id,
    "listing_id": listing_id,
    "score": updated_score,
    "timestamp": timestamp,
}


# СТАЛО
new_state = {
    "actor_id": actor_id,
    "event_id": event_id,
    "score": updated_score,
    "timestamp": timestamp,
}
```

### Sequential evidence

```python
# БЫЛО
def compute_sequential_evidence(seller_events: list[dict], decay: float) -> float:
    for event in seller_events:
        ...


# СТАЛО
def compute_sequential_evidence(actor_events: list[dict], decay: float) -> float:
    for event in actor_events:
        ...
```

Примечание: `sequential` как scoring key не переименовывается в M1.2.

Формулы не изменены: да  
Ключи DEFAULT_WEIGHTS не изменены: да

## app/large_deviation.py

Назначение изменения: сделать statistical deviation нейтральным: `price` → `value`, `category` → `topic`, `region` → `location`. Statistical formulas не менять.

### Value deviation

```python
# БЫЛО
def compute_price_surprise(avg_price_deviation: float, baseline_window: list[float]) -> float:
    z = robust_z_score(avg_price_deviation, baseline_window)
    return gaussian_tail_probability(z)


# СТАЛО
def compute_value_surprise(avg_value_deviation: float, baseline_window: list[float]) -> float:
    z = robust_z_score(avg_value_deviation, baseline_window)
    return gaussian_tail_probability(z)


# DEPRECATED ALIAS — удалить в v3.2
compute_price_surprise = compute_value_surprise
```

### Event surprise

```python
# БЫЛО
def compute_listing_surprise(listing: dict, baseline) -> float:
    avg_price_deviation = listing.get("avg_price_deviation", 0.0)
    category_id = listing.get("category_id")
    region = listing.get("region")
    baseline_window = baseline.get_window(category_id, region)
    return compute_price_surprise(avg_price_deviation, baseline_window)


# СТАЛО
def compute_event_surprise(event: dict, baseline) -> float:
    avg_value_deviation = event.get("avg_value_deviation", 0.0)
    topic_id = event.get("topic_id")
    location = event.get("location")
    baseline_window = baseline.get_window(topic_id, location)
    return compute_value_surprise(avg_value_deviation, baseline_window)


# DEPRECATED ALIAS — удалить в v3.2
compute_listing_surprise = compute_event_surprise
```

Примечание: `surprise` как scoring key не переименовывается в M1.2.

Формулы не изменены: да  
Ключи DEFAULT_WEIGHTS не изменены: да

## app/trusted_baseline.py

Назначение изменения: trusted baseline переводится с sellers на actors.

### Class rename

```python
# БЫЛО
class TrustedSellerBaseline:
    def __init__(self, anchor_sellers: set[str]):
        self.anchor_sellers = anchor_sellers


# СТАЛО
class TrustedActorBaseline:
    def __init__(self, anchor_actors: set[str]):
        self.anchor_actors = anchor_actors


# DEPRECATED ALIAS — удалить в v3.2
TrustedSellerBaseline = TrustedActorBaseline
```

### Trusted collection

```python
# БЫЛО
def build_from_trusted_sellers(self, trusted_sellers: list[dict]) -> dict:
    for seller in trusted_sellers:
        seller_id = seller["seller_id"]
        rating = seller.get("rating")
        reviews_count = seller.get("reviews_count")
        ...


# СТАЛО
def build_from_trusted_actors(self, trusted_actors: list[dict]) -> dict:
    for actor in trusted_actors:
        actor_id = actor["actor_id"]
        reputation_score = actor.get("reputation_score")
        endorsement_count = actor.get("endorsement_count")
        ...


# DEPRECATED ALIAS — удалить в v3.2
build_from_trusted_sellers = build_from_trusted_actors
```

### Anchor check

```python
# БЫЛО
def is_anchor_seller(self, seller_id: str) -> bool:
    return seller_id in self.anchor_sellers


# СТАЛО
def is_anchor_actor(self, actor_id: str) -> bool:
    return actor_id in self.anchor_actors


# DEPRECATED ALIAS — удалить в v3.2
is_anchor_seller = is_anchor_actor
```

Формулы не изменены: да  
Ключи DEFAULT_WEIGHTS не изменены: да

## app/security.py

Назначение изменения: только доменные поля в payload. HMAC, canonical JSON, SHA256, CSV injection protection не трогать.

### Payload fields

```python
# БЫЛО
def canonicalize_score_payload(seller_id: str, listing_id: str | None, score_payload: dict) -> str:
    payload = {
        "seller_id": seller_id,
        "listing_id": listing_id,
        "score_payload": score_payload,
    }
    return canonical_json(payload)


# СТАЛО
def canonicalize_score_payload(actor_id: str, event_id: str | None, score_payload: dict) -> str:
    payload = {
        "actor_id": actor_id,
        "event_id": event_id,
        "score_payload": score_payload,
    }
    return canonical_json(payload)


# DEPRECATED ALIAS — удалить в v3.2
canonicalize_seller_score_payload = canonicalize_score_payload  # [REQUIRES_VERIFICATION]
```

Если старая функция называлась иначе, deprecated alias должен соответствовать фактическому старому имени. [REQUIRES_VERIFICATION]

### Audit event payload

```python
# БЫЛО
audit_payload = {
    "seller_id": seller_id,
    "listing_id": listing_id,
    "category_id": category_id,
}


# СТАЛО
audit_payload = {
    "actor_id": actor_id,
    "event_id": event_id,
    "topic_id": topic_id,
}
```

### Не менять

```python
# НЕ ТРОГАТЬ В M1.2
SYSTEM_META = ...
```

```python
# НЕ ТРОГАТЬ В M1.2
hmac.new(...)
hashlib.sha256(...)
```

Формулы не изменены: да  
Ключи DEFAULT_WEIGHTS не изменены: да

## app/explanations.py

Назначение изменения: заменить marketplace phrasing на domain-neutral phrasing. Reason codes, связанные со scoring keys, не переименовывать в M1.2.

### Explanation text

```python
# БЫЛО
REASON_TEXT = {
    "new_seller_high_activity": "New seller has unusually high posting activity.",
    "price_deviation": "Listing price deviates from category baseline.",
    "contact_reuse": "Seller contact identity is reused across multiple accounts.",
}


# СТАЛО
REASON_TEXT = {
    "new_actor_high_activity": "New actor has unusually high event emission activity.",
    "value_deviation": "Event value deviates from topic baseline.",
    "identity_reuse": "Actor identity is reused across multiple entities or contexts.",
}


# DEPRECATED ALIASES — удалить в v3.2
DEPRECATED_REASON_ALIASES = {
    "new_seller_high_activity": "new_actor_high_activity",
    "price_deviation": "value_deviation",
    "contact_reuse": "identity_reuse",
}
```

### Explanation function

```python
# БЫЛО
def explain_seller_score(score_payload: dict) -> str:
    seller_id = score_payload.get("seller_id")
    return f"Seller {seller_id}: {summary}"


# СТАЛО
def explain_actor_score(score_payload: dict) -> str:
    actor_id = score_payload.get("actor_id")
    return f"Actor {actor_id}: {summary}"


# DEPRECATED ALIAS — удалить в v3.2
explain_seller_score = explain_actor_score
```

### Не менять в M1.2

```python
# НЕ ПЕРЕИМЕНОВЫВАТЬ В M1.2:
# scoring reason labels linked to existing scoring keys
# e.g. residue, sequential, graph, trust, robustness
```

Формулы не изменены: да  
Ключи DEFAULT_WEIGHTS не изменены: да

## app/calibration.py

Назначение изменения: calibration inputs/labels переводятся на actors/events, но calibration logic не меняется.

### CSV paths

```python
# БЫЛО
SELLERS_CSV = "data/sellers.csv"
LISTINGS_CSV = "data/listings.csv"


# СТАЛО
ACTORS_CSV = "data/actors.csv"
EVENTS_CSV = "data/events.csv"


# DEPRECATED ALIASES — удалить в v3.2
SELLERS_CSV = ACTORS_CSV
LISTINGS_CSV = EVENTS_CSV
```

### Loaders

```python
# БЫЛО
def load_sellers(path: str = SELLERS_CSV):
    return pd.read_csv(path)


def load_listings(path: str = LISTINGS_CSV):
    return pd.read_csv(path)


# СТАЛО
def load_actors(path: str = ACTORS_CSV):
    return pd.read_csv(path)


def load_events(path: str = EVENTS_CSV):
    return pd.read_csv(path)


# DEPRECATED ALIASES — удалить в v3.2
load_sellers = load_actors
load_listings = load_events
```

### Calibration function

```python
# БЫЛО
def calibrate_seller_scores(sellers, listings):
    for _, seller in sellers.iterrows():
        seller_listings = listings[listings["seller_id"] == seller["seller_id"]]
        result = score_seller(seller.to_dict(), seller_listings.to_dict("records"))
        ...


# СТАЛО
def calibrate_actor_scores(actors, events):
    for _, actor in actors.iterrows():
        actor_events = events[events["actor_id"] == actor["actor_id"]]
        result = score_actor(actor.to_dict(), actor_events.to_dict("records"))
        ...


# DEPRECATED ALIAS — удалить в v3.2
calibrate_seller_scores = calibrate_actor_scores
```

Формулы не изменены: да  
Ключи DEFAULT_WEIGHTS не изменены: да

## app/api.py

Назначение изменения: добавить новые domain-neutral endpoints и сохранить старые как deprecated aliases.

### Request schema

```python
# БЫЛО
class ScoreRequest(BaseModel):
    seller_id: str
    listing_id: str | None = None
    category_id: str | None = None
    features: dict = {}


# СТАЛО
class ScoreRequest(BaseModel):
    actor_id: str
    event_id: str | None = None
    topic_id: str | None = None
    features: dict = {}


class DeprecatedScoreRequest(BaseModel):
    seller_id: str
    listing_id: str | None = None
    category_id: str | None = None
    features: dict = {}
```

Примечание: scoring output не менять в M1.2. `legacy_component_scores`, `canonical_component_scores`, `weights_version`, `calibration_status`, `alpha`, `gamma` не добавлять в M1.2.

### Score endpoint

```python
# БЫЛО
@app.post("/score")
def score(request: ScoreRequest):
    return score_seller_by_id(
        seller_id=request.seller_id,
        listing_id=request.listing_id,
        category_id=request.category_id,
        features=request.features,
    )


# СТАЛО
@app.post("/score")
def score(request: ScoreRequest):
    return score_actor_by_id(
        actor_id=request.actor_id,
        event_id=request.event_id,
        topic_id=request.topic_id,
        features=request.features,
    )
```

### Deprecated score alias

```python
# СТАЛО
@app.post("/score-seller", deprecated=True)
def score_seller_deprecated(request: DeprecatedScoreRequest, response: Response):
    response.headers["Deprecation"] = "true"
    response.headers["Sunset"] = "v3.2"

    migrated = ScoreRequest(
        actor_id=request.seller_id,
        event_id=request.listing_id,
        topic_id=request.category_id,
        features=request.features,
    )
    return score(migrated)
```

### Actor endpoint

```python
# БЫЛО
@app.get("/seller/{seller_id}")
def get_seller(seller_id: str):
    return repository.get_seller(seller_id)


# СТАЛО
@app.get("/actor/{actor_id}")
def get_actor(actor_id: str):
    return repository.get_actor(actor_id)


@app.get("/seller/{seller_id}", deprecated=True)
def get_seller_deprecated(seller_id: str, response: Response):
    response.headers["Deprecation"] = "true"
    response.headers["Sunset"] = "v3.2"
    return get_actor(seller_id)
```

### Event endpoint

```python
# БЫЛО
@app.get("/listing/{listing_id}")
def get_listing(listing_id: str):
    return repository.get_listing(listing_id)


# СТАЛО
@app.get("/event/{event_id}")
def get_event(event_id: str):
    return repository.get_event(event_id)


@app.get("/listing/{listing_id}", deprecated=True)
def get_listing_deprecated(listing_id: str, response: Response):
    response.headers["Deprecation"] = "true"
    response.headers["Sunset"] = "v3.2"
    return get_event(event_id=listing_id)
```

### Repository aliases

```python
# БЫЛО
repository.get_seller(seller_id)
repository.get_listing(listing_id)


# СТАЛО
repository.get_actor(actor_id)
repository.get_event(event_id)


# DEPRECATED ALIASES — удалить в v3.2
repository.get_seller = repository.get_actor  # [REQUIRES_VERIFICATION]
repository.get_listing = repository.get_event  # [REQUIRES_VERIFICATION]
```

Формулы не изменены: да  
Ключи DEFAULT_WEIGHTS не изменены: да

## app/main.py

Назначение изменения: CLI/demo переводится на actors/events, но старые flags сохраняются.

### CLI arguments

```python
# БЫЛО
parser.add_argument("--sellers", default="data/sellers.csv")
parser.add_argument("--listings", default="data/listings.csv")


# СТАЛО
parser.add_argument("--actors", default="data/actors.csv")
parser.add_argument("--events", default="data/events.csv")

# DEPRECATED ALIASES — удалить в v3.2
parser.add_argument("--sellers", default=None, help="Deprecated alias for --actors")
parser.add_argument("--listings", default=None, help="Deprecated alias for --events")
```

### Path resolution

```python
# СТАЛО
actors_path = args.actors if args.sellers is None else args.sellers
events_path = args.events if args.listings is None else args.listings
```

### Demo run

```python
# БЫЛО
def run_demo(sellers_path: str, listings_path: str):
    sellers = load_csv(sellers_path)
    listings = load_csv(listings_path)
    for seller in sellers:
        seller_listings = [
            row for row in listings
            if row["seller_id"] == seller["seller_id"]
        ]
        print(score_seller(seller, seller_listings))


# СТАЛО
def run_demo(actors_path: str, events_path: str):
    actors = load_csv(actors_path)
    events = load_csv(events_path)
    for actor in actors:
        actor_events = [
            row for row in events
            if row["actor_id"] == actor["actor_id"]
        ]
        print(score_actor(actor, actor_events))
```

Формулы не изменены: да  
Ключи DEFAULT_WEIGHTS не изменены: да

## app/dashboard.py

Назначение изменения: UI labels и CSV paths становятся domain-neutral.

### Title and data loading

```python
# БЫЛО
st.title("Entropy-RG Seller Anomaly Detector")
sellers = pd.read_csv("data/sellers.csv")
listings = pd.read_csv("data/listings.csv")


# СТАЛО
st.title("Entropy-RG Actor/Event Anomaly Detector")
actors = pd.read_csv("data/actors.csv")
events = pd.read_csv("data/events.csv")
```

### Selectors

```python
# БЫЛО
selected_seller = st.selectbox("Seller", sellers["seller_id"].tolist())
seller = sellers[sellers["seller_id"] == selected_seller].iloc[0].to_dict()
seller_listings = listings[listings["seller_id"] == selected_seller].to_dict("records")
result = score_seller(seller, seller_listings)


# СТАЛО
selected_actor = st.selectbox("Actor", actors["actor_id"].tolist())
actor = actors[actors["actor_id"] == selected_actor].iloc[0].to_dict()
actor_events = events[events["actor_id"] == selected_actor].to_dict("records")
result = score_actor(actor, actor_events)
```

### Metrics

```python
# БЫЛО
st.metric("Seller risk score", result["final_score"])
st.dataframe(seller_listings)


# СТАЛО
st.metric("Actor risk score", result["final_score"])
st.dataframe(actor_events)
```

Примечание: scoring output panel не менять в M1.2, кроме доменных labels.

Формулы не изменены: да  
Ключи DEFAULT_WEIGHTS не изменены: да

## app/state_store.py

Назначение изменения: state storage identifiers переводятся на actor/event. Структуру не расширять.

### Sequence state

```python
# БЫЛО
def get_sequence_state(self, seller_id: str):
    return self.conn.execute(
        "SELECT * FROM sequence_state WHERE seller_id = ?",
        (seller_id,),
    )


# СТАЛО
def get_sequence_state(self, actor_id: str):
    return self.conn.execute(
        "SELECT * FROM sequence_state WHERE actor_id = ?",
        (actor_id,),
    )
```

```python
# БЫЛО
def save_sequence_state(self, seller_id: str, state: dict):
    state["seller_id"] = seller_id
    ...


# СТАЛО
def save_sequence_state(self, actor_id: str, state: dict):
    state["actor_id"] = actor_id
    ...
```

### DDL

```sql
-- БЫЛО
CREATE TABLE IF NOT EXISTS sequence_state (
    seller_id TEXT PRIMARY KEY,
    state_json TEXT NOT NULL,
    updated_at TEXT NOT NULL
);


-- СТАЛО
CREATE TABLE IF NOT EXISTS sequence_state (
    actor_id TEXT PRIMARY KEY,
    state_json TEXT NOT NULL,
    updated_at TEXT NOT NULL
);
```

### Migration note

Если база уже существует, нужен SQL migration:

```sql
ALTER TABLE sequence_state RENAME COLUMN seller_id TO actor_id;
```

Если в таблицах есть `listing_id`, применить:

```sql
ALTER TABLE sequence_events RENAME COLUMN listing_id TO event_id;
```

Формулы не изменены: да  
Ключи DEFAULT_WEIGHTS не изменены: да

## scripts/migrate_csv_domain_neutral.py

Назначение изменения: новый скрипт для конвертации CSV с backup-логикой.

```python
# СТАЛО
from __future__ import annotations

import shutil
import sys
from datetime import datetime
from pathlib import Path

import pandas as pd


DATA_DIR = Path("data")
LEGACY_DIR = DATA_DIR / "legacy"

SELLERS_TO_ACTORS = {
    "seller_id": "actor_id",
    "seller_name": "actor_name",
    "category": "topic",
    "category_id": "topic_id",
    "region": "location",
    "reviews_count": "endorsement_count",
    "rating": "reputation_score",
    "account_age_days": "actor_age_days",
    "verified": "verified_actor",
    "posting_frequency": "emission_frequency",
    "contact_reuse_score": "identity_reuse_score",
    "trusted_seller": "trusted_actor",
    "anchor_seller": "anchor_actor",
}

LISTINGS_TO_EVENTS = {
    "listing_id": "event_id",
    "seller_id": "actor_id",
    "category": "topic",
    "category_id": "topic_id",
    "region": "location",
    "price": "value",
    "avg_price_deviation": "avg_value_deviation",
    "posting_frequency": "emission_frequency",
    "duplicate_text_ratio": "duplicate_text_ratio",
    "duplicate_image_ratio": "duplicate_media_ratio",
}


def backup_file(path: Path) -> Path:
    if not path.exists():
        raise FileNotFoundError(f"Missing input file: {path}")

    LEGACY_DIR.mkdir(parents=True, exist_ok=True)
    timestamp = datetime.utcnow().strftime("%Y%m%dT%H%M%SZ")
    backup_path = LEGACY_DIR / f"{path.name}.{timestamp}.bak"
    shutil.copy2(path, backup_path)
    return backup_path


def migrate_csv(input_name: str, output_name: str, mapping: dict[str, str]) -> dict:
    input_path = DATA_DIR / input_name
    output_path = DATA_DIR / output_name

    backup_path = backup_file(input_path)

    df = pd.read_csv(input_path)
    original_columns = list(df.columns)

    rename_map = {old: new for old, new in mapping.items() if old in df.columns}
    preserved_columns = [col for col in original_columns if col not in rename_map]

    migrated = df.rename(columns=rename_map)
    migrated.to_csv(output_path, index=False)

    report = {
        "input": str(input_path),
        "output": str(output_path),
        "backup": str(backup_path),
        "rows": len(migrated),
        "renamed_columns": len(rename_map),
        "preserved_columns": len(preserved_columns),
        "renamed": rename_map,
        "preserved": preserved_columns,
    }
    return report


def main() -> int:
    try:
        reports = [
            migrate_csv("sellers.csv", "actors.csv", SELLERS_TO_ACTORS),
            migrate_csv("listings.csv", "events.csv", LISTINGS_TO_EVENTS),
        ]

        for report in reports:
            print(f"Migrated: {report['input']} -> {report['output']}")
            print(f"Backup: {report['backup']}")
            print(f"Rows: {report['rows']}")
            print(f"Renamed columns: {report['renamed_columns']}")
            print(f"Preserved columns: {report['preserved_columns']}")
            print(f"Renamed map: {report['renamed']}")
            print(f"Preserved: {report['preserved']}")
            print("---")

        return 0

    except Exception as exc:
        print(f"CSV migration failed: {exc}", file=sys.stderr)
        return 1


if __name__ == "__main__":
    raise SystemExit(main())
```

Формулы не изменены: да  
Ключи DEFAULT_WEIGHTS не изменены: да

## tests/

Назначение изменения: test fixtures переводятся на actor/event, но numeric expected score должен совпадать.

### Fixture rename

```python
# БЫЛО
seller = {
    "seller_id": "seller_001",
    "account_age_days": 10,
    "rating": 4.2,
    "reviews_count": 12,
    "verified": False,
    "posting_frequency": 0.8,
    "contact_reuse_score": 0.4,
}


# СТАЛО
actor = {
    "actor_id": "actor_001",
    "actor_age_days": 10,
    "reputation_score": 4.2,
    "endorsement_count": 12,
    "verified_actor": False,
    "emission_frequency": 0.8,
    "identity_reuse_score": 0.4,
}
```

```python
# БЫЛО
listing = {
    "listing_id": "listing_001",
    "seller_id": "seller_001",
    "category_id": "phones",
    "region": "UA-KYIV",
    "avg_price_deviation": 0.5,
    "duplicate_text_ratio": 0.2,
    "duplicate_image_ratio": 0.1,
}


# СТАЛО
event = {
    "event_id": "event_001",
    "actor_id": "actor_001",
    "topic_id": "phones",
    "location": "UA-KYIV",
    "avg_value_deviation": 0.5,
    "duplicate_text_ratio": 0.2,
    "duplicate_media_ratio": 0.1,
}
```

### Public function alias test

```python
# СТАЛО
def test_deprecated_score_seller_alias_matches_score_actor():
    assert score_seller is score_actor
```

[REQUIRES_VERIFICATION] If alias is implemented as wrapper instead of direct assignment, compare outputs instead of identity.

Формулы не изменены: да  
Ключи DEFAULT_WEIGHTS не изменены: да

## Изменения в данных

## data/sellers.csv → data/actors.csv

### Полный маппинг колонок

| Старый столбец | Новый столбец | Статус |
|---|---|---|
| `seller_id` | `actor_id` | required |
| `seller_name` | `actor_name` | optional [OPEN_QUESTION] |
| `category` | `topic` | optional |
| `category_id` | `topic_id` | optional |
| `region` | `location` | optional |
| `reviews_count` | `endorsement_count` | optional |
| `rating` | `reputation_score` | optional |
| `account_age_days` | `actor_age_days` | optional |
| `verified` | `verified_actor` | optional |
| `posting_frequency` | `emission_frequency` | optional |
| `contact_reuse_score` | `identity_reuse_score` | optional |
| `trusted_seller` | `trusted_actor` | optional [OPEN_QUESTION] |
| `anchor_seller` | `anchor_actor` | optional [OPEN_QUESTION] |

### Пример первых 3 строк нового формата

```csv
actor_id,actor_name,topic,location,endorsement_count,reputation_score,actor_age_days,verified_actor,emission_frequency,identity_reuse_score
actor_001,Reference Actor A,electronics,UA-KYIV,128,4.8,940,true,0.18,0.02
actor_002,Reference Actor B,phones,UA-LVIV,12,3.9,21,false,0.76,0.41
actor_003,Reference Actor C,cars,UA-ODESA,43,4.2,180,true,0.33,0.10
```

### Конвертер

Использовать `scripts/migrate_csv_domain_neutral.py` из секции выше.

## data/listings.csv → data/events.csv

### Полный маппинг колонок

| Старый столбец | Новый столбец | Статус |
|---|---|---|
| `listing_id` | `event_id` | required |
| `seller_id` | `actor_id` | required |
| `category` | `topic` | optional |
| `category_id` | `topic_id` | optional |
| `region` | `location` | optional |
| `price` | `value` | optional [OPEN_QUESTION] |
| `avg_price_deviation` | `avg_value_deviation` | optional |
| `posting_frequency` | `emission_frequency` | optional |
| `duplicate_text_ratio` | `duplicate_text_ratio` | unchanged |
| `duplicate_image_ratio` | `duplicate_media_ratio` | optional |
| `created_at` | `created_at` | unchanged |
| `updated_at` | `updated_at` | unchanged |
| `title` | `title` | unchanged |
| `description` | `description` | unchanged |

### Пример первых 3 строк нового формата

```csv
event_id,actor_id,topic_id,topic,location,value,avg_value_deviation,duplicate_text_ratio,duplicate_media_ratio,created_at
event_1001,actor_001,topic_electronics,electronics,UA-KYIV,1200,0.14,0.03,0.01,2026-05-01T10:00:00Z
event_1002,actor_002,topic_phones,phones,UA-LVIV,220,0.71,0.18,0.22,2026-05-01T10:05:00Z
event_1003,actor_003,topic_cars,cars,UA-ODESA,8500,0.39,0.07,0.05,2026-05-01T10:10:00Z
```

### Конвертер

Использовать `scripts/migrate_csv_domain_neutral.py` из секции выше. Скрипт обязан:

- читать старые CSV;
- переименовывать известные колонки;
- сохранять неизвестные колонки без изменений;
- делать backup в `data/legacy/`;
- выводить отчёт о количестве переименованных и сохранённых колонок;
- выводить количество строк;
- завершаться с exit code 0 только при успехе.

## Изменения в API

## Новые endpoints

| Новый endpoint | Назначение |
|---|---|
| `GET /actor/{id}` | Получить actor по id |
| `GET /event/{id}` | Получить event по id |

## Deprecated aliases

| Deprecated endpoint | Новый endpoint | Headers |
|---|---|---|
| `GET /seller/{id}` | `GET /actor/{id}` | `Deprecation: true`, `Sunset: v3.2` |
| `GET /listing/{id}` | `GET /event/{id}` | `Deprecation: true`, `Sunset: v3.2` |

## Request schemas

### POST /score

```python
# БЫЛО
class ScoreRequest(BaseModel):
    seller_id: str
    listing_id: str | None = None
    category_id: str | None = None
    features: dict = {}


# СТАЛО
class ScoreRequest(BaseModel):
    actor_id: str
    event_id: str | None = None
    topic_id: str | None = None
    features: dict = {}
```

### Deprecated POST /score-seller

```python
# СТАЛО
class DeprecatedScoreRequest(BaseModel):
    seller_id: str
    listing_id: str | None = None
    category_id: str | None = None
    features: dict = {}
```

## Response schemas

В M1.2 response меняет только доменные поля. Scoring output не меняется.

```json
{
  "actor_id": "actor_001",
  "event_id": "event_001",
  "topic_id": "topic_phones",
  "component_scores": {
    "gibbs": 0.0,
    "residue": 0.0,
    "sequential": 0.0,
    "surprise": 0.0,
    "graph": 0.0,
    "trust": 0.0,
    "robustness": 0.0
  },
  "final_score": 0.0
}
```

Примечание: пример содержит scoring keys только для демонстрации того, что они не меняются в M1.2. Их переименование запрещено до M1.3.

## Изменения в документации

## README.md

### Абзац описания

```markdown
# БЫЛО
Entropy-RG detects suspicious sellers and listings on a marketplace.


# СТАЛО
Entropy-RG detects anomalous actors and events in domain-neutral data streams.

Marketplace-style data remains available only as a reference-domain example:
seller -> actor, listing -> event, category -> topic, region -> location.
```

### Раздел данных

```markdown
# БЫЛО
The demo reads sellers from data/sellers.csv and listings from data/listings.csv.


# СТАЛО
The demo reads actors from data/actors.csv and events from data/events.csv.

Deprecated legacy file names are supported only through the migration script
and compatibility aliases until v3.2.
```

### API section

```markdown
# БЫЛО
GET /seller/{id}
GET /listing/{id}


# СТАЛО
GET /actor/{id}
GET /event/{id}

Deprecated aliases retained until v3.2:
GET /seller/{id}
GET /listing/{id}
```

## docs/TECHNICAL_SPECIFICATION.md

### Domain model section

```markdown
# БЫЛО
The core entities are seller, listing, category and region.


# СТАЛО
The core entities are actor, event, topic and location.

- actor: entity that emits or participates in events.
- event: atomic observation received from a source.
- topic: thematic grouping used for baselines and coupling profiles.
- location: geographic or logical context.
```

### Reference-domain note

```markdown
# СТАЛО
Marketplace terms are deprecated in the core model. They may appear only in
reference-domain examples and compatibility notes. The migration mapping is:

seller -> actor
listing -> event
category -> topic
region -> location
```

### Scoring note

```markdown
# СТАЛО
M1.2 does not change scoring keys, scoring weights, formulas, thresholds,
confluence logic or system metadata. Canonical scoring rename is handled in M1.3.
```

## Тест-чеклист M1.2

## 1. Проверить отсутствие старых имён в основном коде

```bash
grep -R "seller\|listing\|category\|region" -n app data tests README.md docs || true
```

Допустимые места:

- deprecated aliases;
- compatibility adapters;
- reference-domain examples;
- migration scripts;
- comments with explicit `Deprecated` or `reference-domain`.

Для более строгой проверки:

```bash
grep -R "seller_id\|listing_id\|category_id\|reviews_count\|account_age_days\|avg_price_deviation\|posting_frequency\|duplicate_image_ratio\|contact_reuse_score" -n app tests README.md docs || true
```

Допустимые места:

- deprecated request schemas;
- CSV migration mapping;
- compatibility tests.

## 2. Проверить наличие actors.csv и events.csv

```bash
test -f data/actors.csv
test -f data/events.csv
```

Проверить обязательные колонки:

```bash
python - <<'PY'
import pandas as pd

actors = pd.read_csv("data/actors.csv")
events = pd.read_csv("data/events.csv")

required_actor_cols = {"actor_id"}
required_event_cols = {"event_id", "actor_id"}

missing_actor = required_actor_cols - set(actors.columns)
missing_event = required_event_cols - set(events.columns)

assert not missing_actor, f"Missing actor columns: {missing_actor}"
assert not missing_event, f"Missing event columns: {missing_event}"

print("CSV required columns OK")
PY
```

## 3. Проверить row count старых и новых CSV

Если legacy backups сохранены в `data/legacy/`, выбрать последние backup-файлы:

```bash
python - <<'PY'
from pathlib import Path
import pandas as pd

legacy_dir = Path("data/legacy")
sellers_backup = sorted(legacy_dir.glob("sellers.csv.*.bak"))[-1]
listings_backup = sorted(legacy_dir.glob("listings.csv.*.bak"))[-1]

old_sellers = pd.read_csv(sellers_backup)
old_listings = pd.read_csv(listings_backup)
actors = pd.read_csv("data/actors.csv")
events = pd.read_csv("data/events.csv")

assert len(old_sellers) == len(actors), (len(old_sellers), len(actors))
assert len(old_listings) == len(events), (len(old_listings), len(events))

print("CSV row counts OK")
PY
```

## 4. Проверить pytest

```bash
pytest -q
```

Ожидание:

```text
all tests passed
```

## 5. Проверить score equality

До миграции должен быть сохранён baseline:

```bash
python app/main.py --sellers data/sellers.csv --listings data/listings.csv > /tmp/v2_2_scores.jsonl
```

После миграции:

```bash
python app/main.py --actors data/actors.csv --events data/events.csv > /tmp/v3_0_m1_2_scores.jsonl
```

Сравнение:

```bash
python - <<'PY'
import json
from pathlib import Path

def load_jsonl(path):
    rows = []
    for line in Path(path).read_text().splitlines():
        line = line.strip()
        if not line:
            continue
        rows.append(json.loads(line))
    return rows

old = load_jsonl("/tmp/v2_2_scores.jsonl")
new = load_jsonl("/tmp/v3_0_m1_2_scores.jsonl")

assert len(old) == len(new), (len(old), len(new))

for i, (a, b) in enumerate(zip(old, new)):
    assert abs(a["final_score"] - b["final_score"]) < 1e-9, (i, a["final_score"], b["final_score"])

print("Score equality OK")
PY
```

Если CLI output не JSONL, адаптировать parser. Критерий остаётся:

```text
final_score после M1.2 равен final_score v2.2 на эквивалентных данных
```

## 6. Проверить API endpoints

Запустить API:

```bash
uvicorn app.api:app --reload --port 8000
```

Проверить новый endpoint:

```bash
curl -i -s http://localhost:8000/actor/actor_001
```

Ожидание:

- HTTP 200;
- response содержит `actor_id`;
- response не содержит `seller_id`.

Проверить deprecated alias:

```bash
curl -i -s http://localhost:8000/seller/actor_001
```

Ожидание:

- HTTP 200;
- header `Deprecation: true`;
- header `Sunset: v3.2`;
- response содержит `actor_id`.

Проверить event:

```bash
curl -i -s http://localhost:8000/event/event_001
curl -i -s http://localhost:8000/listing/event_001
```

## 7. Проверить, что scoring keys не переименованы

```bash
grep -R "\"gibbs\"\|'gibbs'\|\"residue\"\|'residue'\|\"sequential\"\|'sequential'\|\"surprise\"\|'surprise'\|\"graph\"\|'graph'\|\"trust\"\|'trust'\|\"robustness\"\|'robustness'" -n app tests || true
```

Ожидание:

- эти ключи всё ещё присутствуют в scoring code/tests;
- нет новых canonical scoring keys в M1.2:
  - `rg_persistence_score`;
  - `sequential_anomaly_score`;
  - `graph_risk`;
  - `trust_penalty` как scoring rename.

Примечание: `trust_penalty` как внутренняя переменная уже может существовать по ревью, но M1.2 не должен менять её статус. [REQUIRES_VERIFICATION]

## 8. Проверить, что DEFAULT_WEIGHTS не изменён

Сохранить diff:

```bash
git diff -- app/scoring.py | sed -n '/DEFAULT_WEIGHTS/,/}/p'
```

Ожидание:

- нет изменений в `DEFAULT_WEIGHTS`.

## Ограничения M1.2

В этом patch запрещено:

- переименовывать scoring keys;
- добавлять dual output;
- добавлять `canonical_component_scores`;
- добавлять `legacy_component_scores`;
- добавлять `weights_version`;
- добавлять `calibration_status`;
- добавлять `alpha`;
- добавлять `gamma`;
- создавать `app/canonical_mapping.py`;
- менять `DEFAULT_WEIGHTS`;
- менять формулы;
- менять числовые пороги;
- менять HMAC-логику;
- менять `SYSTEM_META`;
- удалять deprecated aliases;
- механически заменять строки без контекстной проверки;
- менять значения CSV-строк;
- менять reference-domain keys внутри `TOPIC_COUPLING`.

## Критерии приёмки M1.2

M1.2 считается готовым, если:

1. Все доменные имена из таблицы переименований отражены в patch-секциях.
2. Для каждой переименованной public function/method добавлен deprecated alias.
3. В конце каждой файловой секции указано:
   - «Формулы не изменены: да»;
   - «Ключи DEFAULT_WEIGHTS не изменены: да».
4. Скрипт `scripts/migrate_csv_domain_neutral.py` содержит backup-логику.
5. Тест-чеклист содержит исполняемые bash-команды.
6. Раздел «Обнаруженные дополнительные имена» присутствует.
7. Scoring keys не переименованы.
8. `DEFAULT_WEIGHTS` не изменён.
9. `SYSTEM_META` не изменён.
10. Score equality с v2.2 подтверждён.
